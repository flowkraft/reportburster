# Reference 🔧

This section contains concise, copy/paste reference material for implementers.

**Planned content (v0)**
- Tabulator Options DSL snippets (columns, layoutOptions, pagination)
- Chart config examples (datasets, type, colors)
- Pivot config examples (rows, columns, aggregations)
- Web component attributes & methods (`report-code`, `api-base-url`, `api-key`, `fetchData()`)
- API endpoints: `/api/jobman/reporting/reports/{reportCode}/config` and `/data`

We will expand each subsection with examples and mappings to the Groovy DSL.
