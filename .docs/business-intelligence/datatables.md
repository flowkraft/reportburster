# Data Tables (Tabulator) 📋

Use `rb-tabulator` to display, explore and export report data—fast and interactive.

## Key features
- Fast rendering and virtual scrolling for large datasets
- Sorting, filtering, grouping and pagination
- Column formatters (money, date) and custom renderers
- Inline editing (optional) and export to CSV/XLSX

## Quick example

```html
<rb-tabulator report-code="sales-summary" api-base-url="https://server/api/jobman/reporting" api-key="MY_KEY"></rb-tabulator>
```

See **Reference** for the Tabulator Options DSL (compact, server-driven config examples).
